#include iostream
#include fstream
#include string
using namespace std;
int main () {
	ifstream input (output.txt);
  	string number;
  	int n = 1;
  	while (getline(input, number)){
  		coutn. numberendl;
  		n++;
	}
  	return 0;
}
